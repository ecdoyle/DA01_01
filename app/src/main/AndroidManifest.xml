<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emiliedoyle.da01_01" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FindPage"
            android:label="@string/title_activity_find_page" >
        </activity>
        <activity
            android:name=".AddPage"
            android:label="@string/title_activity_add_page" >
        </activity>
        <activity
            android:name=".SearchResultsPage"
            android:label="@string/title_activity_search_results_page" >
        </activity>
    </application>

</manifest>
